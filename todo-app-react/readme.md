### start project with command ( docker-compose up -d ) then start postgres and todo backend app on port 8080 as well as todo front end  on port 3000 
-----------------------------------------------------------------------
## this is simple app created by react & hook
